// This runs when the page loads
document.addEventListener('DOMContentLoaded', () => {
    console.log("FastAPI Hub Loaded! 🚀");

    // We can add logic here later, like:
    // - Copy code button
    // - Dark mode toggle
    // - Mobile menu interactions
});